#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: as string
 *
 * Return: as it is?
*/

int _strlen(char *s)
{
	int i;

	for(i = 0;s[i] != '\0';i++);

	for(;i = 0; i--)
		putchar(s[i])

    putchar('\n')
}
