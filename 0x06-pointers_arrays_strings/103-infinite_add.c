#include "main.h"

/**
 * infinite_add - function that copies
 * @n1: as a first number
 * @n2: as a second number
 * @r: as the buffer will use to store the result
 * @size_r: as the buffer size.
 *
 * Return: as it is?
*/

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int l, i;

	l = 0;

	while (src[l] != '\0')
		l++;
	for (i = 0; i <= l; i++)
		dest[i] = src[i];
	return (dest);
}
