#include "main.h"
/**
 * mul - a function that multiple
 * @x: as first number input
 * @y: as second number input
 * and returns the result
 * Return: x + y
 */
int mul(int x, int y)
{
	return (x * y);
}
