#include "main.h"
/**
 * add - a function that adds y integers and returns the result
 * @x: first number input
 * @y: second number input
 * Return: x + y
 */
int add(int x, int y)
{
	return (x + y);
}